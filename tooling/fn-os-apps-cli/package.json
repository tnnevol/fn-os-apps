{
  "name": "@tnnevol/fn-os-apps-cli",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Release, build and repository maintenance CLI for fn-os-apps.",
  "bin": {
    "fn-apps-cli": "lib/index.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json",
    "check": "pnpm run typecheck && pnpm run test",
    "prepare": "tsdown",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@clack/prompts": "catalog:",
    "bumpp": "catalog:",
    "changelogithub": "catalog:",
    "commander": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
