[
  {
    "stepTitle": "Bitwarden 基础配置",
    "items": [
      { "type": "text", "field": "DOMAIN", "label": "域名", "initValue": "", "helpText": "Bitwarden 服务器域名，可留空使用 IP。", "rules": [{ "pattern": "^(https?://.*)?$", "message": "域名需以 http:// 或 https:// 开头，或留空" }] },
      { "type": "switch", "field": "WEB_VAULT_ENABLED", "label": "启用 Web 管理界面", "initValue": "true" },
      { "type": "text", "field": "APP_PORT", "label": "访问端口", "initValue": "40031", "rules": [{ "required": true, "message": "请输入端口号" }, { "pattern": "^[0-9]+$", "message": "端口号必须是数字" }] }
    ]
  }
]
