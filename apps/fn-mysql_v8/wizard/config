[
  {
    "stepTitle": "MySQL 基础配置",
    "items": [
      { "type": "password", "field": "MYSQL_ROOT_PASSWORD", "label": "Root 密码", "initValue": "", "helpText": "修改密码后请保存并重启 MySQL。", "rules": [{ "min": 6, "max": 50, "message": "密码长度 6-50 个字符" }] },
      { "type": "text", "field": "APP_PORT", "label": "MySQL 端口", "initValue": "3306", "rules": [{ "required": true, "message": "请输入端口号" }, { "pattern": "^[0-9]+$", "message": "端口号必须是数字" }] },
      { "type": "select", "field": "MYSQL_REMOTE_ACCESS", "label": "允许连接来源", "initValue": "localhost", "options": [{ "label": "仅本机（localhost）", "value": "localhost" }, { "label": "任意远程主机", "value": "any" }] }
    ]
  }
]
