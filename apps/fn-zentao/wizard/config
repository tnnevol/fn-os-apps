[
  {
    "stepTitle": "禅道配置",
    "items": [
      { "type": "text", "field": "ZENTAO_HTTP_PORT", "label": "Web 访问端口", "initValue": "8890", "rules": [{ "required": true, "message": "请输入端口号" }, { "pattern": "^[0-9]+$", "message": "端口号必须是数字" }] }
    ]
  }
]
