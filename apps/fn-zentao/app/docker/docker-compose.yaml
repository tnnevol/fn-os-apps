
services:
    zentao:
        container_name: fn-zentao
        environment:
            - MYSQL_INTERNAL=true
        image: hub.zentao.net/app/zentao:22.1
        ports:
            - ${ZENTAO_HTTP_PORT}:80
        restart: always
        volumes:
            - ${TRIM_PKGVAR}/data:/data
