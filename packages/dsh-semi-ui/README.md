# @tnnevol/dsh-semi-ui

Shared Semi Design components and the DSH theme bridge used by DSH plugins.

The package keeps Semi UI's runtime dependencies and DSH token mapping in one
place so plugins can use the same components without maintaining separate theme
overrides.
