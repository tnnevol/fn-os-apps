/**
 * Repository-wide ambient declaration entry point.
 *
 * Keep declarations specific to a package in that workspace's `src/types/`
 * directory. Add only declarations shared by root-level tooling here.
 */
export {}
