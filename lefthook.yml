commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    staged-lint:
      run: pnpm exec lint-staged

pre-push:
  commands:
    repository-check:
      run: pnpm run check -- --all
