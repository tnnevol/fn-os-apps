---
feature: FNOS-004-08
acceptance: FNOS-004-08-AC-01
environment: DSH 客户端（浏览器实测）
dshVersion: 0.1.5-rc.2
pluginVersion: '@tnnevol/dsh-codex-auth@0.1.5-rc.2; @tnnevol/dsh-codebuddy@0.1.5-rc.2'
verifiedAt: 2026-09-13
status: passed
---

# FNOS-004-08 用量图标供应商显隐 —— DSH 客户端验收记录

## 环境与方法

- 环境：当前 DSH 客户端（浏览器实测），DSH 基线 `0.1.5-rc.2`。
- 被测实现：`35f0e70 feat(plugin): gate usage icons on the selected model provider`。
- 位置：会话输入框右侧（`conversation.input.right`）。
- 方式：在 DSH 界面内切换选中模型的供应商，观察该座位上的用量图标。

本记录只覆盖客户端行为。本次**未**在真实 NAS 上重建并安装 FPK，因此不声称任何 NAS 结论；`dsh-fnos`、网关和 FPK 安装链不在 FNOS-004-08 的范围内。

## 已通过用例

| 验收条件 | 验证结果 |
| --- | --- |
| `FNOS-004-08-AC-01` | 通过。选中 Codex 模型时只出现 Codex 用量图标，选中 CodeBuddy 模型时只出现 CodeBuddy 图标，两家图标互斥。 |

## 未手工复现的验收条件

以下条件本次**没有**在浏览器里逐条复现，不因 AC-01 通过而自动视为已验收。它们目前只有自动化证据：

| 验收条件 | 现有证据 | 状态 |
| --- | --- | --- |
| `FNOS-004-08-AC-02` | 组件按 React 订阅路径重算，无缓存首次结果的逻辑；`usage-visibility` 真值表单测覆盖 `next`/`lastUsed` 折叠 | 待人工复现（切换即时性、草稿不丢失） |
| `FNOS-004-08-AC-03` | 真值表单测覆盖未选模型、投影缺失、`next`/`lastUsed` 皆空、第三家供应商四种分支 | 待人工复现 |
| `FNOS-004-08-AC-04` | 真值表单测覆盖 `showUsage` 关闭时压过供应商匹配 | 待人工复现（关闭「显示额度余量」后选中 CodeBuddy 模型） |
| `FNOS-004-08-AC-05` | 接线断言要求早退早于首次拉取与 `timer.interval`；Codex 61 条、CodeBuddy 656 条单测通过，两插件 bundle 构建通过 | 轮询抑制已由结构断言覆盖，未在网络面板实测 |

## 自动化证据

- Codex Auth：typecheck 通过，13 个测试文件 / 61 条通过（基线 47 条）。
- CodeBuddy：typecheck 通过，656 条通过（基线 641 条）。
  既存的 `tests/token-stats-rpc-guard.spec.ts` 7 条失败与本功能无关（`connection service is unavailable`，源于 `auth-service.ts` 读取注入的 `connectionCtx.connection`），保持原状。
- `pnpm run build -- --plugin codex` / `--plugin codebuddy` 均成功。
- `pnpm run check -- --all`、`pnpm run build -- --docs`、`git diff --check` 通过（唯一失败仍是上述 7 条既存项）。

## 遗留问题与下一步

- AC-02 / AC-03 / AC-04 建议在浏览器中各补一次手工复现，尤其是 AC-04（关掉 `showUsage` 后选中 CodeBuddy 模型仍不显示）——它验证的是「偏好与供应商条件取与」，与 AC-01 是不同的失败模式。
- AC-05 的「隐藏时不轮询」目前靠源码结构断言守住，没有在网络面板上实测请求数量；如需更强证据，可在浏览器 Network 里观察隐藏状态下是否仍有 `usage` 端点请求。
- 要在 NAS 上看到本功能，需重建 FPK（`pnpm run build -- --fpk --app fn-deepseek-harness --bundle-dsh-plugins`）并安装；本次未执行。

## 结论

`FNOS-004-08-AC-01` 在 DSH 客户端的浏览器实测中通过，用户确认功能验收通过。其余验收条件只有自动化证据，按上表记录为待人工复现，不随本次通过一并认作已验收。
