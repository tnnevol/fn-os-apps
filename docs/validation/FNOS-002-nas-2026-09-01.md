---
feature: FNOS-002
acceptance: FNOS-002-P1-NAS-2026-09-01
environment: fnOS NAS
fnosVersion: 真实 NAS 页面环境（版本未在本次浏览器验收中读取）
appVersion: fn-deepseek-harness 5.1.x
pluginVersion: '@tnnevol/dsh-fnos@0.1.1-rc.2; @tnnevol/dsh-codex-auth@0.1.1-rc.2'
verifiedAt: 2026-09-01
status: passed
---

# FNOS-002 真实 NAS 浏览器验收记录

## 环境与方法

- 入口：`http://192.168.119.6:5666/`
- 页面：fnOS 桌面中的 `fn-deepseek-harness` iframe。
- 工具：Chrome，使用 NAS 页面自身的 DOM、iframe 和 Network 列表验证。
- 账号状态：Codex 已登录，页面返回五小时和每周用量。

## 已通过用例

| 用例 | 验证结果 |
| --- | --- |
| `FNOS-002-TP-001-TC-001` | 已登录状态和 Codex 用量入口可见，Popover 可打开。 |
| `FNOS-002-TP-001-TC-002` | 会话输入区显示已登录用户的用量入口。 |
| `FNOS-002-TP-005-TC-001` | 设置中的用量刷新请求返回 `200 application/json`，五小时和每周窗口均展示。 |
| `FNOS-002-TP-005-TC-002` | 刷新后会话输入区保留五小时用量入口，设置面板保留两个窗口。 |
| `FNOS-002-TP-024-TC-001` | 点击左侧“重启”后 DSH Web 恢复，外层 NAS 页面保持可用。恢复约 15 秒。 |
| `FNOS-002-TP-024-TC-002` | 重启后 DSH 会话和用量入口恢复，NAS 外层页面和 iframe 仍存在。 |
| `FNOS-002-TP-032-TC-002` | Network 请求 `/app/fn-deepseek-harness/dsh-pet-7340/pic/cursor-grab.png` 返回 `200`、`image/png`，大小 `303` 字节。 |

## 其他浏览器验证

- 用量 Popover 再次点击可关闭，`aria-expanded=false`，浮层与 Tooltip 互斥。
- NAS TreeSelect 展示 4 个授权目录，复选框可选中和取消。
- `Control + ,` 和 `Meta + ,` 均可打开设置面板。
- `F5` 只刷新 DSH iframe，NAS 外层 URL 保持不变，页面随后恢复。
- 侧边栏折叠后刷新、重启按钮保持居中。
- “跟随系统 → 浅色”切换后浅色状态正确。
- 未启用的 Semi UI 菜单未出现在当前插件菜单中。

## 未覆盖范围

未在本次真实浏览器环境构造鉴权失败、接口字段错误、Tree 引用删除、SSE/WebSocket、FPK 安装升级回滚、并发重启、权限差异和审计日志等场景。这些用例继续保持“未记录”或“待 NAS 验证”，不因本记录通过而自动通过。

## 结论

本次已执行的 7 条业务用例通过。静态资源验证必须使用页面 Network 中实际生成的完整网关路径；`cursor-grab.png` 返回图片类型正常，之前使用缺少网关前缀或错误文件名的测试结果不作为验收依据。
