---
feature: FNOS-002
acceptance: FNOS-002-P1-NAS-destructive-2026-09-01
environment: fnOS NAS
entrypoint: http://192.168.119.6:5666/
verifiedAt: 2026-09-01
status: failed
---

# FNOS-002 破坏性浏览器测试记录

## 执行范围

本次只执行需要修改 NAS/DSH 运行状态、配置或插件状态的用例。测试均从 NAS 桌面中的 DeepSeek Harness FPK 窗口进入，没有直接打开本地 DSH Web。用户已授权执行破坏性操作。

## 实际执行结果

| 用例 | 结果 | 证据与说明 |
| --- | --- | --- |
| `FNOS-002-TP-024-TC-001` | 失败 | 快速连续点击“重启”后 DSH iframe 消失，等待约 16 秒仍未自动恢复；NAS 外层页面保持可用，但必须从应用中心再次点击“打开”才能恢复 DSH。 |
| `FNOS-002-TP-024-TC-002` | 部分通过 | FPK 应用仍显示已安装，重新打开后 4 个授权目录仍在；但自动恢复失败，因此整体不满足用例预期。 |
| `FNOS-002-TP-025-TC-001` | 失败 | 连续点击重启未观察到明确的处理中锁定或去重提示，且第一次执行导致 iframe 消失；无法证明只保留一个重启任务。 |
| `FNOS-002-TP-029-TC-001` | 失败 | 在 fnos 插件配置中输入 `not-an-absolute-path` 后点击“保存”，页面提示“反代规则已保存”，非法规则未被拦截。随后已将规则恢复为空。 |
| `FNOS-002-TP-030-TC-001` | 通过 | 输入临时规则 `/temporary/test` 后点击“放弃”，编辑区恢复为空，未保留未保存内容。 |

## 未执行或前置条件不满足

- `FNOS-002-TP-014` 至 `TP-019`：当前环境没有会话和可删除的引用，且 API Key 配置被跳过，不能构造安全的测试数据。
- `FNOS-002-TP-021-TC-003`：Semi UI 插件当前不在插件列表，无法执行卸载后验证。
- `FNOS-002-TP-025-TC-002`、`TP-025-TC-003`、`TP-026`、`TP-027`：需要健康检查超时、启动失败、并发锁或进程级故障注入，不能仅通过当前页面安全构造。
- `FNOS-002-TP-028`、`TP-029-TC-002`、`TP-029-TC-003`、`TP-030-TC-002`、`TP-031`：需要读取/破坏配置文件或控制上游服务，当前页面没有安全的隔离测试入口。
- `FNOS-002-TP-036` 至 `TP-039`：需要安装源、升级包、校验失败或回滚场景；NAS 应用中心当前没有提供本次测试所需的隔离升级操作。
- `RISK-001` 至 `RISK-010` 中依赖接口篡改、并发、权限切换、SSE/WebSocket 或审计日志的用例：需要后端测试夹具或多账号环境，标记为待专用环境执行。

## 状态恢复与影响

- 未删除文件、目录、会话或插件。
- API URL 反代编辑区已恢复为空，并验证放弃临时编辑不会写入。
- NAS 外层页面保持可用；DeepSeek Harness 通过应用中心重新打开后可加载，4 个授权目录仍显示。
- 重启后浏览器控制台记录到 `web-runtime connection lost` 重试告警，以及 `assets/index-JQ_7u_lS.js` 的 `Object` 错误；这些日志与自动恢复异常一并保留为问题线索。

## 结论

本次已执行当前 NAS 页面上能够安全构造的破坏性操作，但未全部通过。已确认两个需要修复的问题：DSH Web 重启后不能稳定自动恢复，以及 API URL 反代配置接受非法相对路径。其余依赖会话数据、故障注入、安装升级源或权限环境的用例不能在当前 NAS 页面中安全完成，不能标记为通过。
